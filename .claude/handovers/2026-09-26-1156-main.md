# Session Handover — 2026-09-26 11:56 — Skingenetix (CLIENT-003) — main

Session `268cd741` ("skingenetix-website-7c"), 2026-09-24 14:35 → 2026-09-26 11:56.

## 1. Mission

Make the five ingredient science pages the credible, convincing answer in Google and AI search. That means moving each page onto the Argireline-derived science-page template, in six languages, with:

- positive, sourced results only;
- the ingredient's own accent colour;
- ingredient-named image files;
- before/after USPs that Malcolm picks.

## 2. Current cursor

- **Plan:** `docs/science-page-template.md`: §6 is the rollout procedure, §6.1 the lessons, §7 the per-page gaps. The status line says "Copper peptide done 2026-09-26".
- **Tracker:** `docs/todo.md` → CONTENT-001.
- **Position in the rollout:**
  - PDRN ✅, Argireline ✅ (all its cards, including the forehead before/after on f5), copper ✅ live (2026-09-26).
  - **Matrixyl 3000 and glutathione are next. Each needs Malcolm's go-ahead.**
- **Copper is live** with:
  - before/after on cards f1 (crow's feet, Malcolm's pick A2) and f2 (face cream, E2);
  - the page's original collagen-lattice image on f4;
  - verify-live ✓ ×6.
- ⚠️ **THREE Claude sessions worked in this repo on 2026-09-26.** Two of them were both named "skingenetix-website-f3": the study-page/auditor session `99ff47ca` and a copper session `ac56663f`.
  - `ac56663f` ran "design-critic cycle 2" on live copper at about 11:45: a chart narrow-screen reflow, and the key figures and CTA copy moved into the new specs.
  - It committed all of that at 11:57: `5ba7d25` (copper cycle 2 plus `hub_charts.py` and tests), `f2429dc` (Matrixyl register §8, a claims check done ahead of
    any Matrixyl go-ahead) and `6a5c539` (CLAUDE.md, AGENTS.md, README and the rules docs: Impact theme, six locales, `.env` rather than Bitwarden).
  - My work survived its apply (checked live: f1/f2 before/after, the f4 image).
  - Its `docs/architecture.md` 2026-09-26 row covers the copper rollout. My tool changes are not in it yet (see §10).
- **Spec gate / MCW:** N/A (content work).

## 3. Next concrete action

Get Malcolm's answers to the open decisions in §10. Start with the copper third key figure and the caveat sheet, and carry each answer out inside the §6 envelope.

## 4. Verification recipe

```bash
cd "/Users/malcolmsmith/Claude Code/Projects/skingenetix-website"
git status -sb                                   # check for other sessions' staged files: commit only your own paths (git commit -- <paths>)
python3 -m pytest tests/ -q                      # 63 passed
python3 scripts/hub-i18n.py configs/hub-i18n/<page>.json          # "all checks passed"; covers custom-html AND card/FAQ block targets
python3 scripts/hub-upgrade.py configs/hub-upgrades/<spec>.json   # dry run; order-after = template §2
python3 scripts/hub-upgrade.py configs/hub-upgrades/<spec>.json --apply
python3 scripts/hub-upgrade.py configs/hub-upgrades/<spec>.json --verify-live   # ✓ ×6
```

- **Numbers across languages:** `scripts/harvest-translations.py` exposes `numbers_agree(en, other)`. Run it over every phrase: 0 mismatches expected.
- **Captures:** Playwright with a **full Chrome user agent**.
  - Block `klaviyo`, `forms.shopifyapps.com` and `shopify-forms`, and set `.shopify-section--header` to `position:static`.
  - Tile the captures to `~/Desktop/skingenetix-renders.png` and run `open` on it.
- **Label fit:** measure each `rba__label` / `rba__result` bounding box inside its `img` at 1440 and 390 in all six languages. The script from this session was in the scratchpad (`label_fit.py`), so rewrite it if needed.

## 5. Stop conditions (ask permission before)

- **Rollouts:** moving Matrixyl 3000 or glutathione onto the template (one go-ahead per page).
- **Content not yet ruled on:**
  - any caveat-sentence change on PDRN or Argireline until Malcolm answers the caveat sheet;
  - any template-level design change (the six proposals) until he chooses.
- **Spend:** new image-generation waves. Every image goes to every supplier (Malcolm's rule).
- **The "1%" image:** replacing it on `/pages/collagen-skin-plumping` needs Malcolm's OK or pick.
- **Audits:** don't re-run the external SEO/AISO audits until the other session's F-011 auditor fix lands.
- **Other sessions' files:** committing, reverting or reformatting anything another session has staged.
- **Standing blocks:** menus; product titles, prices and inventory; core Liquid; deleting pages, products or files; emails; new accounts.

## 6. Autonomy envelope (already approved — proceed without asking)

- All `.md` edits and commits.
- Committing and pushing docs, scripts, configs and tests (explicit paths only).
- Shopify content work on the science pages within the template: specs, translations, SEO, theme JSON, and uploads under ingredient-named files.
- Applying Malcolm's before/after picks to their cards.
- Fixing page-level defects found by verification or the design critic, when the fix stays inside the template.
- Artifact publishing, and memory updates.
- **Git checks are ON** (`.husky/_` shims marked executable, Malcolm's permission 2026-09-25). Never use `--no-verify`. See §8 for how to pass them.

## 7. Decisions log (don't re-debate)

- **Standard sections vs custom code, tested live** (template §5; memory `stock-richtext-and-section-css-limits`).
  - Shopify richtext refuses `class`/`style`/`<table>`/`<div>` (upload 422).
  - A section's native Custom CSS is capped at 500 characters and refuses `content:`.
  - So the template's four custom-html sections are forced by the approved design. Stock plus Custom CSS is only for small tweaks.
- **Copper follows the approved Argireline template**, with the same section ids, `--sg-accent:#014EB1` and hero overlay 50%.
- **Copper content:** Pickart & Margolina 2018 dropped. The Mortazavi 2024 "undoubtedly GHK is effective on wrinkle reduction" line sits in "Independent evidence". Key figures unchanged (−24.1%, 7 in 10, 2026 review), pending Malcolm on figure 3.
- **Copper before/after** (Malcolm, 2026-09-26): a new round-3-style batch, not the 27 Aug candidates. Card 2 is the 71-woman face-cream study.
  - Picks: A2 `gpt_image 01` for f1 (her right side = the trial's treated side) and E2 `gpt_image 01` for f2.
  - Result lines: f1 "Wrinkle volume −24.1% vs −15.0% on the plain serum base (8-week trial)", f2 "Firmer-looking skin, fewer fine lines (face-cream study, 71 women)".
- **Copper f4 image** (Malcolm): `skingenetix-copper-peptide-collagen-lattice-density-research.jpg`, his 2026-08-25 pick for this finding, which matches f3.
- **Argireline f5 before/after** (Malcolm's B4 `nbp_flash 01`): Before / After 20 days, with "Forehead roughness −7.4% vs +4.3% on placebo (day 20)". Raikou's figures were re-checked at source.
- **"Zurück zur Wissenschaft"** (not "zu") in all three phrase tables, live on PDRN and Argireline.
- **The Dutch CTA "Shop … Serum"** is consistent on all five science pages. Left as it is; offered "Koop…" to Malcolm.
- **The auditor fix (F-011) is owned by the other session**, which already had Malcolm's "proceed". Malcolm's wider instruction to me was forwarded to it verbatim.

## 8. Constraints learned this session

- **Git checks and the lint backlog.**
  - The pre-commit hook runs `markdownlint --fix` plus `prettier` on staged `.md/.json/.yml`. The repo has about 685 markdownlint issues and 512 unformatted files, which is why CI has been red on every push.
  - Fix each `.md` when you touch it:
    1. run `npx markdownlint --fix <f>`;
    2. soft-wrap lines over 300 characters at word boundaries, outside code spans, link URLs, tables and headings (rendering is unchanged);
    3. run `npx prettier --write <f>`;
    4. confirm the wording is unchanged by comparing the text with whitespace and emphasis marks stripped.
  - Inline HTML (MD033) can't be auto-fixed. `docs/architecture.md` has 7 such issues.
- **Shared staging area.** When another session has files staged, commit with an explicit pathspec (`git commit -m … -- <paths>`), so its staged files stay out.
- **`hub-i18n.py --write` re-saves specs with `json.dumps` formatting**, and prettier then reformats them on commit. Check any big diff for meaning (compare the JSON, not the text).
- **Cloudflare:** after heavy traffic even curl with a bare `Mozilla/5.0` gets a 403 "Verifying your connection" page. Use a full Chrome user agent, and check the body size or title before trusting an empty search.
- **Preview via `?view=`** (memory `preview-a-page-template-with-view-before-live`).
  - Stock-section translations don't show on a preview.
  - The first apply to a new template can fail translation registration. Re-run it.
- **Cut-outs:** `rembg.new_session` hangs. Run `~/.u2net/isnet-general-use.onnx` with onnxruntime directly (0.7 s). Save as WebP with alpha, and upload through the Files API (`upload-theme-images.py` forces JPEG).
- **Luma refuses face briefs sometimes.** It returned HTTP 422 on `cub1--copper-facecream-e`.
- **Engines ignore lower-face crops.** Every one returned a whole three-quarter face. Keep the masters square, because the card needs it.
- **Old off-brand images in Files.** `skingenetix-copper-peptide-ghk-cu-firming-repair-serum.jpg` shows a "1%" copper serum and is live on `templates/page.collagen-skin-plumping.json`.
- **Copper JSON-LD descriptions** in de/nl/fr/es/it used to overstate ("55.8% … than without"). They are corrected in `configs/hub-i18n/copper-peptide-research.json` `jsonld_i18n`.

## 9. Failed approaches (don't retry)

- **`rembg` `new_session("birefnet-general")`:** hung for over 15 minutes at 0% CPU.
- **A probe template with block type `text` in rich-text:** the correct type is `richtext`.
- **Extracting phrase units with `<(th)…>`:** it matched `<thead>`. Use `\b` after the tag name.
- **Recognising the byline as "By Skingenetix":** the translated bylines differ, so alignment broke. Match the byline by its shape instead: `<p><em>[^<]*Skingenetix\.[^<]*</em></p>`.
- **Editing Python raw strings with `sed`:** it broke the quoting. Use the Edit tool.
- **The August copper before/after candidates:** they predate the plain-wall rule, and Malcolm chose a new batch.

## 10. Open questions / awaiting human

The sheets are on Malcolm's Desktop.

1. **Six design proposals** (`~/Desktop/skingenetix-design-proposals{,-1..-6}.png`, `docs/design-proposals-2026-09-25.md`). The options:
   - body text 17/16 px;
   - phone H1 vs key figures;
   - PDRN hero: one of 27 existing banners, a new wave, or keep the current one;
   - usage-image variety;
   - one grading scale (A·B·C·D·Review);
   - drop the collapsible table.

   Also: copper's third key figure, "2026" (a year, flagged by the critic). The suggestion is "Week 4: wrinkle volume already 17.2% lower than at the start".

2. **24 caveat sentences on PDRN and Argireline** (`~/Desktop/skingenetix-caveats{,-pdrn,-argireline}.png`, `docs/caveat-decisions-2026-09-25.md`). "Accept all recommendations" is a valid answer. Whatever he answers:
   - fix Argireline FAQ "cumulative benefits beyond 28 days" (unsupported);
   - fix Argireline card 4, which cites Henseler (no longer in the table);
   - C18 needs Argireline's chart moved out of a retired spec first.
3. **The "1%" copper image** on `/pages/collagen-skin-plumping`: may it be replaced with the day gel-cream jar (`skingenetix-copper-peptide-ghk-cu-day-gel-cream-jar-botanical-wood.jpg`)?
4. **The auditor review sheet** (other session): `seo-toolkit/docs/audits/criteria-review-2026-09-25.md`. After F-011 lands, re-run the external audits on PDRN, Argireline and copper with the `seo-aiso-validator` skill.
5. **Go-ahead for Matrixyl 3000**, then glutathione. The other session has a `docs/claims/matrixyl-3000.md` change staged, so check it before starting.
6. **Optional:** the Dutch CTA wording "Koop…" instead of "Shop…" on all five pages.
7. **`docs/architecture.md` is missing this session's tool changes.** Add a 2026-09-26 row: `scripts/harvest-translations.py` (new, 7 tests),
   `scripts/hub-i18n.py` block and set targets (3 tests), `scripts/build-copper-before-after-config.py` (new), husky pre-commit enabled, and the stock-section
   limits (template §5). The file needs its lint backlog fixed to pass the git check (87 over-long lines, 22 MD038, 7 MD033).
8. **Carried from earlier sessions:**
   - Dr Bodde's confirmation of her review;
   - the parked formula facts;
   - the "gentle / well tolerated" sweep;
   - Evidence Library wave 1;
   - the repo-wide markdown lint backlog (CI red).

## 11. Plan & sub-plan references (read fresh; do not inline)

- **Template and rollout:** `docs/science-page-template.md`, including the §5 stock-section test table.
- **Standard:** `docs/content-plan-2026.md` §4. **Tracker:** `docs/todo.md` → CONTENT-001. **ADRs:** `docs/decisions-log.md` (ADR-2026-09-24-S, -P).
- **Claims registers:**
  - `docs/claims/copper-peptide-ghk-cu.md` §8 (2026-09-25 verification log);
  - `docs/claims/argireline-acetyl-hexapeptide-8.md` §6 (Raikou re-check);
  - `docs/claims/pdrn.md`.
- **Copper specs:** `configs/hub-upgrades/copper-peptide-research-{layout,evidence-merge}-2026-09-25.json`. Phrase table: `configs/hub-i18n/copper-peptide-research.json`.
- **Before/after:**
  - builder `scripts/build-copper-before-after-config.py` (imports the round-3 builder);
  - wave `configs/banners/before-after-copper-cards-r1.json`;
  - upload plan `configs/banners/copper-peptide-research-card-images-2026-09-26.json`;
  - Argireline f5 plan `configs/banners/acetyl-hexapeptide-8-research-card-images-2026-09-25.json`.
- **Decision docs:** `docs/design-proposals-2026-09-25.md`, `docs/caveat-decisions-2026-09-25.md`.
- **Audits:** `docs/audits/pages/README.md`. The copper critic scored FIX 6.5 on cycle 1; cycle 2 was run by the other session.

## 12. File state (mechanical)

- **Committed this session:**
  - `scripts/`: `harvest-translations.py` (new), `hub-i18n.py` (block targets), `build-copper-before-after-config.py` (new).
  - `tests/`: `test_harvest_translations.py` (new), `test_hub_i18n.py` (+3).
  - `configs/hub-upgrades/`: `copper-peptide-research-{layout,evidence-merge}-2026-09-25.json` (new), `acetyl-hexapeptide-8-evidence-merge-2026-09-23.json` (f5), the `pdrn-research-layout` and `acetyl-hexapeptide-8-layout` specs (German fix), and the copper parity and retrofit specs (`_retired`).
  - `configs/hub-i18n/`: `copper-peptide-research.json` (new), plus `pdrn-research.json` and `acetyl-hexapeptide-8-research.json` (German fix).
  - `configs/banners/`: `before-after-copper-cards-r1.json`, `copper-peptide-research-card-images-2026-09-26.json`, `acetyl-hexapeptide-8-research-card-images-2026-09-25.json`.
  - `configs/reviewers/esther-bodde.json` (copper hub → layout spec, `overview.html`, `evidence_sources`).
  - `docs/`: `todo.md`, `science-page-template.md`, `claims/argireline-acetyl-hexapeptide-8.md`, `claims/copper-peptide-ghk-cu.md`, `audits/pages/README.md`, `design-proposals-2026-09-25.md` and `caveat-decisions-2026-09-25.md` (both new).
- **Local only (gitignored):**
  - `assets/web-sources/` (copper cut-out source, the before/after sources);
  - `assets/ai-generated/2026-08-22-multi-before-after-copper-cards-r1/` (35 candidates);
  - `backups/hub-upgrade-*2026092[56]*`.
- **Live-store changes this session:**
  - copper template (applied several times; backups in `backups/`);
  - Argireline f5;
  - German overview fix on PDRN and Argireline;
  - new Files: the copper cut-out WebP, 3 before/after JPEGs, the Argireline forehead JPEG;
  - probe and preview templates created and **deleted**.
- **Memory added:** `stock-richtext-and-section-css-limits`, `preview-a-page-template-with-view-before-live`, `rembg-hangs-run-the-onnx-model-directly`. Updated: `cloudflare-throttles-python-not-curl` (user-agent addendum), and the `MEMORY.md` index trimmed to under 17.1 KB.

## 13. Git state

- **Branch:** `main`.
- **Last commit before this handover:** `a0425a7`, "copper: card 4 gets the page's original collagen-lattice image, matching card 3".
- **Pushed:** yes (main = origin/main).
- **After this handover commit:** nothing tracked is uncommitted. `LATEST.md` is a symlink kept untracked on purpose (the pre-commit formatter refuses symlinks, and the start-of-session hook reads the local file).
- **Other sessions' commits after mine:** `5ba7d25`, `f2429dc` and `6a5c539` (the `ac56663f` session, 11:57).

## 14. Session log (append-only)

- **09-24 14:35:** primed, with three reader agents (docs, history, code).
  - Found that the other session is the study-pages one.
  - Re-checked Raikou's day-20 forehead figures in the full text (`3f3bd98`).
- **09-25:**
  - Malcolm's picks and instructions arrived.
  - Argireline f5 went live: B4, with non-breaking spaces so the label wraps cleanly (`bf28080`).
  - Enabled the husky hooks, then fixed `todo.md` to pass the gate.
  - Measured the stock-section limits with probe templates (`11a5c22`).
  - Wrote the translation harvester with tests.
  - Helpers produced the design-proposal and caveat sheets (`876cc80`, `d98bdbb`).
  - Stopped my auditor agent and handed F-011 to the other session.
- **09-25:**
  - Copper claims check (`7e42d63`).
  - Retired the stale copper specs (`56c9f98`).
  - hub-i18n block targets (`64bfba8`).
  - Copper cut-out (ISNet).
  - Specs built and translated (30 phrases reused, 53 new, via a translation helper) (`7ecfda0`).
  - Preview via `?view=`.
  - Design critic FIX 6.5, fixed: hero overlay, neutral card images, f1 length, the German "zur" (`28c3ca9`).
- **09-26:**
  - Copper live on Malcolm's request (`c95d726`), verified ×6.
  - Copper before/after wave: smoke test 12/12, full run 35/36 (`8f5a01b`, `dda26b0`).
  - Picks A2 and E2 applied live, labels measured in six languages (`73686d0`).
  - f4 image swapped (`a0425a7`).
  - Translation sweep: nothing missing.

## 15. Continuation prompt

Paste verbatim into the new session:

> Read `/Users/malcolmsmith/Claude Code/Projects/skingenetix-website/.claude/handovers/2026-09-26-1156-main.md` and continue from §3.
> First run `git status -sb`. Another Claude session has staged copper, docs and chart changes: never commit them; commit only your own paths with `git commit -- <paths>`.
> Read `docs/science-page-template.md` and ADR-2026-09-24-P fresh. Positive results only, the ingredient's accent and ingredient-named image files are standing rules. The git pre-commit checks are ON: never `--no-verify`, fix each touched `.md` to pass them (§8).
> Operate within §6. Halt at any §5 stop condition and ask. Validate live in six languages at desktop and mobile, and show renders on the Desktop contact sheet.
> Begin with a one-line confirmation of the cursor (§2), then execute §3.
