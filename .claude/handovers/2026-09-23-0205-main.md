# Session Handover — 2026-09-23 02:05 — Skingenetix (CLIENT-003) — main

## 1. Mission

Make skingenetix.com findable, credible and convincing in Google and AI search: every page carries the strongest claim its evidence supports, worded for conversion, correct in all six languages, and built to Malcolm's research-page standard.

## 2. Current cursor

- Plan file: `/Users/malcolmsmith/Claude Code/Projects/skingenetix-website/docs/content-plan-2026.md` — §4 (Phase 2) is **complete**; §5 (Phase 3, the spokes) is next. Tracked in `docs/todo.md` → CONTENT-001.
- Position: **Phase 2 → 5 of 5 hubs done.** The glutathione hub (`/pages/glutathione-research`) was rebuilt this session and audits 100/100/100/100, external 9.65. One Phase 2 loose end remains: the "at a glance" front-loading lift on the PDRN and copper hubs (both lack the `<h3>At a glance</h3>` list that Matrixyl and glutathione carry).
- Spec gate / MCW: N/A (content work)

## 3. Next concrete action

Add the "at a glance" list (What it is / Best evidence / Independent evidence / Not shown / Safety / How to use) after the definition paragraph on the PDRN and copper hubs, as **additive** retrofit specs (`configs/hub-upgrades/pdrn-research-at-a-glance-2026-09-2x.json`, `copper-peptide-research-at-a-glance-…json`, one `set` item each on `overview/op/content` taken from the **live** value, never from the stale original specs), in six languages, then re-run `page-audit.py` on both. That closes Phase 2. Then start Phase 3 with the first PDRN spoke per `docs/content-plan-2026.md` §5.

## 4. Verification recipe

```bash
cd "/Users/malcolmsmith/Claude Code/Projects/skingenetix-website"
python3 -m pytest tests/ -q                                                    # expect 24 passed
python3 scripts/hub-upgrade.py configs/hub-upgrades/<spec>.json                # dry run: six locales, prefixed hrefs, h2 counts
python3 scripts/hub-upgrade.py configs/hub-upgrades/<spec>.json --apply
python3 scripts/hub-upgrade.py configs/hub-upgrades/<spec>.json --verify-live  # expect ✓ en de nl fr es it
python3 scripts/page-audit.py /pages/pdrn-research /pages/copper-peptide-research   # expect 100/100/100, MARKETING ≥ 85
python3 scripts/link-audit.py                                                   # "without locale prefix" must be 0
```

Run storefront jobs **one at a time** (429s otherwise). For any page change: screenshot desktop + mobile with the capture recipe in §8, tile to `~/Desktop/skingenetix-renders.png` and `open` it (standing rule).

## 5. Stop conditions (ask permission before)

- **Menu structure changes.** Malcolm approved the label "Science" only.
- Evidence Library **wave 1** (library page, 4 more studies incl. Watanabe 2014, product→study links): proposed, not approved.
- Any spend beyond normal volume: the DataForSEO `llm_responses` AI-citation panel; more than a few `aiso-audit-page.py` rounds per page.
- **Product claims that need the parked formula facts (§10).** The trial magnitudes for glutathione (−10.7%, 77%) and Matrixyl (−39%, +15%) stay on the hubs until the certificate confirms the levels.
- Core Liquid edits; deleting pages, products or collections; unpublishing or redirecting (e.g. the collagen-skin-plumping consolidation).
- Product titles (they sync to Kaufland and Bol), prices, inventory.
- AI or illustration disclosure on before/after images (Malcolm: "No AI disclosure please").
- Changing the before/after **labels** ("Before" / "After 10 weeks") beside study numbers — Malcolm's open question of 2026-09-22, never answered. The result labels were changed (they stated an unmeasured outcome); the time labels were not.
- Publishing `/pages/reviews` (deliberately unpublished); emails; new accounts; `chmod +x` on the husky hooks.

## 6. Autonomy envelope (already approved — proceed without asking)

- All `.md` edits and commits; commit and push docs, scripts, configs and tests.
- Shopify content work: SEO titles and descriptions, page/section JSON, translations, collection and product descriptions and metafields (not titles), FAQ metaobjects, theme JSON, `study` metaobjects.
- Phase 2 hub work and retrofits (Malcolm: "proceed with phase 2"); Phase 3 articles per the plan (English first, then German, for rewrites of existing text; new text in all six languages at once).
- **Strongest-claims copy rewrite** wherever the marketing audit is unhappy, with deeper claims research if needed; removing claims found unsupported at source, in all six languages.
- Artifact publishing (standing).

## 7. Decisions log (don't re-debate)

- **Research-page standard (Malcolm, 2026-09-22)** — every science page: external dual-model audit (pass 9.0) plus `page-audit.py`; stock Impact sections first; several existing images; charts of the key data with a real `<table>` under each; every link checked. Matrixyl and glutathione are the two reference builds. Memory: `research-page-standard`.
- **Published citation titles are quoted verbatim (ADR-2026-09-23-G).** The old glutathione hub had rewritten four titles to hide "skin-whitening". Titles are restored; `page-audit.py` exempts only the References title element (`.sgref__ti`) from the EU wording scan. A paper's term quoted in our prose still counts — the chart caption was reworded, not exempted.
- **Ingredient-level only on products until the formula is confirmed.** Applied on glutathione exactly as on Matrixyl: the Clinical Research block states design, direction ("brighter-looking skin than placebo, p < 0.001"), timing and tolerability, and links to the hub for the numbers. Memory: `in-vivo-magnitudes-stay-off-products-until-the-formula-is-confirmed`.
- **Per-hub review date.** `set-reviewer.py` takes `"reviewed"` on a hub entry (`hub_cfg`); glutathione's `lastReviewed` is 2026-09-23, the other four keep 2026-09-22. Schema.org `lastReviewed` is the page's content-check date.
- **`impact-text` big figures render as `<h2>` — accepted again.** Gemini scored heading hierarchy 6/10 on glutathione for it (the page's only failing criterion besides the open named-author question). The standard names `impact-text` for headline figures and the alternative is custom code. Do not spend an audit round on it.
- **A findings run of `research-before-after` + `media-with-text` in either order is exempt from the alternation check.** Neither section has a background setting; the glutathione trio (rba → mwt → rba) must stay adjacent for `findings_spacing_fix`.
- **Reviewer credited before her review (ADR-2026-09-22-R)** — now on five hubs and two study pages; Malcolm is confirming with Dr Bodde. Remove everywhere with `python3 scripts/set-reviewer.py configs/reviewers/esther-bodde.json --remove --apply`.
- **Brightening is cosmetic.** Never "whitening", "inhibits melanin", "even tone", "dark spots" or "radiance" as glutathione claims; oral/IV glutathione is grade X and never supports a topical product. Topical and oral are kept apart on the hub.
- Earlier decisions still stand: the blog is `/blogs/learn/`; the research pages are the hubs; concern pages and collections kept but differentiated; the three older hub specs (`pdrn-`, `acetyl-hexapeptide-8-`, `copper-peptide-research.json`) are stale and must never be re-applied — retrofit additively from the live value.

## 8. Constraints learned this session

- **[VERIFIED] The template is `templates/page.glutathione-research.json`**, not `page.research-glutathione.json` as the previous handover said. The five hub templates are `page.pdrn-research`, `page.research-argireline`, `page.research-copper-peptide`, `page.research-matrixyl`, `page.glutathione-research`.
- **[VERIFIED] `add_sections` can replace an existing section wholesale** (it removes the id and re-inserts after `after`), which is how the references block was rebuilt with corrected titles without touching `hub-upgrade.py`; a plain-string `html` is not lifted for translation, and `references_i18n` + `jsonld` then localise it.
- **[VERIFIED] A translation registered seconds ago can read as `outdated` once.** `set-reviewer.py` flagged `it: faq.q3.title` right after the hub apply; a re-query a minute later showed nothing outdated. Re-query before re-registering. Memory: `a-just-registered-translation-can-read-as-outdated-once`.
- **[VERIFIED] `fix-claims.py` is atomic and its dry run is the safety net**: it refused round 2 because the science page's schema block is translated with *translated* sentences (not the English JSON-LD), and wrote nothing until the locale patterns matched. Fetch the translated sentence before writing a locale pattern.
- **[VERIFIED] Meta descriptions:** `seo-apply.py` rejects an English description over 160; it does **not** check translations, so measure them yourself (≤ 155 is the page-quality bar; the first glutathione set came back 174–184 and was rewritten).
- **[VERIFIED] Capture recipe** (scratchpad script this session, not committed): Playwright at 1440×900 and 390×844, `await pg.route("**/*klaviyo*", lambda r: asyncio.ensure_future(r.abort()))`, hide `#shopify-pc__banner, .newsletter-popup`, scroll to load lazy images, full-page screenshots, then `ffmpeg … hstack` to `~/Desktop/skingenetix-renders.png` and `open`. Worth committing as `scripts/capture-live.py` next time it is needed.
- Storefront 429s under parallel fetching — one verification job at a time (unchanged).
- The husky hooks are still not executable (markdownlint/Prettier have never run); logged, not fixed.

## 9. Failed approaches (don't retry)

- Rendering the Watanabe melanin-index result as a two-series chart with one row: too thin. Drawn as one series with two rows (GSSG side, placebo side) and the means in the caption; the ratings chart (four rows from Table 3, two series) carries the comparison.
- Quoting the paper's "skin whitening" label in the chart caption to explain the translation: the EU wording scan flags it as our claim. Say "an older lightening term" instead.
- Writing locale patterns for a JSON-LD sentence by assuming the translations carry the English text: the science page's schema translations are translated. The dry run caught it; nothing shipped.
- Running `hub-upgrade.py --apply` twice in one command to count and then show its output: harmless (idempotent) but it uploads twice and leaves a spare backup. Capture the output once.

## 10. Open questions / awaiting human

- **Dr Bodde's confirmation** of the credit now live on five hubs and two study pages — Malcolm is asking her.
- **A named individual author.** Still the only criterion both auditors mark down (Gemini 7 on glutathione). Malcolm's call.
- **Before/after time labels** ("After 10 weeks") beside study numbers — unanswered since 2026-09-22 12:45; not acted on.
- **FOLLOW-UP (parked): formula facts from the supplier certificate** — glutathione 2% GSSG? Matrixyl 3000 % in serum and cream? Argireline grade? Are the Matrixyl and PDRN cream INCI lists swapped? Is the Matrixyl cream vegan?
- **Formula-level claims not assessed by any register:** "Boosted with Vitamin C & Niacinamide — for extra radiance support" (serum benefit), the niacinamide, vitamin C and Nonapeptide-1/Tetrapeptide-1 lines on `/pages/ingredients`, and the skin-concerns FAQ mapping "Brightening: uneven tone and dullness (Glutathione)". Left as they are this session; they need their own sources (niacinamide has real evidence for tone, but the product's % is a parked formula fact).
- **"Well-tolerated" / "gentle" sweep** on the PDRN, Argireline and copper product FAQs and concern pages (the glutathione and Matrixyl ones are done).
- **Evidence Library wave 1** go-ahead (Watanabe 2014 is one of its four study pages), and whether to spend on an AI-citation panel.
- **Product-page GEO template gap** (serum GEO 61: no early definition, ~430 extractable words, no page JSON-LD) — affects every product; a separate workstream (README pattern 4).
- `aiso-audit-page.py` should be centralised into the seo-aiso-validator skill (two copies exist, Rule 12).
- **Husky hooks** — one `chmod +x` away, needs Malcolm's OK.

## 11. Plan & sub-plan references (read fresh; do not inline)

- Build list: `docs/content-plan-2026.md` (§4 research-page standard; §5 Phase 3 spokes)
- Tracker: `docs/todo.md` → CONTENT-001
- Hero map: `docs/keyword-strategy-2026.md`
- Claims registers: `docs/claims/{glutathione,matrixyl-3000,pdrn,copper-peptide-ghk-cu,argireline-acetyl-hexapeptide-8}.md` (glutathione §7 is this session's verification log)
- Evidence Library architecture: `docs/research-2026-study-hubs-credibility.md` §3
- Decisions: `docs/decisions-log.md` (ADR-2026-09-23-G is this session's)
- Audits: `docs/audits/pages/README.md` (scores, the external audit table, the claims-verified table), `docs/audits/pages/aiso-glutathione-research-en-2026-09-23-r1.md`
- Reference builds: `configs/hub-upgrades/matrixyl-3000-research.json`, `configs/hub-upgrades/glutathione-research.json`; additive pattern: `*-retrofit-2026-09-22.json`
- Claims-pass pattern: `configs/claim-fixes/glutathione-2026-09-23.json` + `glutathione-round2-2026-09-23.json`, `configs/copy/clinical-research-glutathione-2026-09-23.json`

## 12. File state (mechanical — do not paraphrase)

- **Committed this session** (`198be6f`):
  - `configs/hub-upgrades/glutathione-research.json` (new, 183 KB, citations linked, reviewer synced)
  - `configs/reviewers/esther-bodde.json` (glutathione hub entry with `"reviewed": "2026-09-23"`)
  - `configs/seo-changes/phase2-glutathione-2026-09-23.json` + `.titles.translations.json` + `.descriptions.translations.json`; `claims-glutathione-products-2026-09-23.json` + `.descriptions.translations.json`; `configs/seo-snapshots/seo-before-*-2026-09-23-*.json` ×2
  - `configs/claim-fixes/glutathione-2026-09-23.json` (16 values), `glutathione-round2-2026-09-23.json` (8 values)
  - `configs/copy/clinical-research-glutathione-2026-09-23.json`
  - `scripts/page-audit.py`, `scripts/set-reviewer.py`; `tests/test_page_audit.py` (new), `tests/test_set_reviewer.py`
  - `docs/audits/pages/README.md`, `docs/audits/pages/{pages-glutathione-research,pages-brightening-glow,products-glutathione-brightening-serum}-2026-09-23.{md,json}`, `docs/audits/pages/aiso-glutathione-research-en-2026-09-23-r1.{md,json}`
  - `docs/content-plan-2026.md`, `docs/todo.md`, `docs/decisions-log.md`, `docs/claims/glutathione.md`
- **Live Shopify state (not in git; backups in `backups/`, gitignored):** `templates/page.glutathione-research.json` (3 uploads: `hub-upgrade-…-20260923-012236`, `-013125`, reviewer stamps), `templates/page.brightening-glow.json`, `page.science.json`, `page.skin-concerns.json`, `page.ingredients.json` (claim fixes, backups `20260923-014746`, `-020031`), two products' `custom.clinical_research` (`clinical-…-20260923-014953`), body_html and `custom.key_benefits` on the serum and the Duo, six `faq_item` metaobjects, the `brightening-glow` collection description, page + product SEO fields, ~380 translations.
- **Uncommitted:** the pre-existing untracked PNGs in the repo root; `.claude/handovers/loaded-20260923-005510.md` and the hook's deletion of `2026-09-23-0052-main.md` (committed with this handoff).
- **Memory (outside the repo):** three new files + a compacted `MEMORY.md` (23.6 → 16.4 KB, all 127 entries kept) in `~/.claude/projects/-Users-malcolmsmith-Claude-Code-Projects-skingenetix-website/memory/`.

## 13. Git state

- Branch: `main`
- Last commit: `198be6f` — "phase 2: the glutathione hub to the research-page standard, its claims fixed across the store, and two audit-tool fixes"
- Pushed: **yes**
- Uncommitted diff: only the handover files (this file, LATEST.md, the loaded-… rename), committed as the handoff commit right after this file is written; plus the pre-existing untracked PNGs.

## 14. Session log (append-only)

- **Primed** with `/prime` (four subagents: objectives/rules, recent history, tooling map, Matrixyl reference); confirmed the cursor and executed the previous handover's §3.
- **Verified at source before writing a word:** Watanabe 2014 full-text XML via E-utilities (title, affiliations — three of four authors at Kyowa Hakko Bio — Tables 1–3, melanin-index means, timing of every significant result, safety, mechanism "no more than speculation", GSSG stability), 16 PubMed records by `esummary`, Sonthalia by PMC id, Etnawati at Crossref. No figure in the register was contradicted; Table 3's week-by-week ratings gave the second chart.
- **Glutathione hub rebuilt** (`configs/hub-upgrades/glutathione-research.json`, built by a scratchpad assembler from three part files): 17 `set` items, 2 FAQ blocks added, 6 sections added (evidence, stats, charts, evidence_table, usage, references rebuilt), backgrounds alternated, 14 citations linked in-text, per-locale references and JSON-LD. Dry run clean; applied; 56 keys × 5 locales registered; `--verify-live` ✓ ×6.
- **Round 1 audit** 100/96/90/71 → fixes: split a 137-word paragraph, cited the trial in two key-figure blocks, reworded the caption, and three tool changes with tests → **100/100/100/100, 0 findings**. External dual-model round 1: **9.75 / 9.55 = 9.65** (17 criteria at 10/10 on both; the two below 9 are recorded decisions).
- **Reviewer credit** added to the glutathione hub (config entry + `--apply`), then re-stamped with the per-hub date after the `hub_cfg` change. **SEO** title + description in six languages (page), descriptions shortened to ≤ 155 after the first set ran long.
- **Screenshots** desktop + mobile with Klaviyo blocked → `~/Desktop/skingenetix-renders.png`, opened. Direct six-locale check: chart heading, 2 tables, translated references heading, title, description length, `inLanguage`, `reviewedBy`, `lastReviewed` 2026-09-23, 14 citations, no English leaks — all ✓.
- **Claims pass, round 1** (`fix-claims.py`, 16 values, read-back clean ×6): serum and Duo descriptions and key benefits, four FAQ items, eight `brightening-glow` settings. **Clinical Research block** rebuilt on both products with a `glutathione` kit (+ copper for the Duo). **Product SEO** descriptions rewritten in six languages. Audits after: serum Marketing 93 (GEO 61 = template gap), brightening-glow Marketing 100.
- **Store survey** for banned phrasing → **round 2** (8 values): the collection description ("reducing hyperpigmentation"), two Duo FAQ answers, the science page FAQ and its schema (translated sentences fetched first after the dry run refused), the skin-concerns tile ("dark spots", "post-inflammatory marks"), the ingredients block, the concern page's approach paragraph. Read-back clean ×6.
- **Docs**: audits README (scores row, external table row, seven claims-verified rows), content-plan status (5 of 5, Phase 2 complete), todo (Phase 2 complete + three entries + four line updates), decisions-log ADR-2026-09-23-G, claims register status line + §7 verification log. **Memory**: three new entries; index compacted under the 17 KB limit.
- **Tests** 19 → 24, all green. Committed `198be6f`, pushed.

## 15. Continuation prompt

Paste verbatim into new session:

> Read `/Users/malcolmsmith/Claude Code/Projects/skingenetix-website/.claude/handovers/2026-09-23-0205-main.md` and continue from §3 "Next concrete action".
> FIRST read §1 (mission) and §7 (decisions) so you know the standard every research page must meet, then read the plans in §11 fresh — do not rely on summaries here. Retrofit from the **live** value of `overview/op/content`, never from the stale original PDRN and copper specs.
> Operate within the §6 autonomy envelope. Halt at any §5 stop condition and ask. Ingredient-level claims only; the formula facts are parked (§10).
> Validate live: six languages, desktop and mobile, verification jobs one at a time, and show the screenshots on the Desktop contact sheet.
> Begin with a one-line confirmation of the cursor (§2), then execute.
